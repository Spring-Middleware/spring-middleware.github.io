services:
  spring-middleware-site:
    image: nginx:alpine
    container_name: spring-middleware-site
    ports:
      - "8081:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped